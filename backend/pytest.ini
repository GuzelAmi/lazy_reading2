[pytest]
addopts = -v --strict-markers --tb=short --cov=app --cov-fail-under=60
testpaths = tests
markers =
    unit: Модульные тесты (изолированные, быстрые)
    integration: Интеграционные тесты (требуют БД)
    e2e: Сквозные тесты (требуют браузер)